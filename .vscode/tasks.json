{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Application",
            "type": "shell",
            "command": "${workspaceFolder}/run.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}